name: transcriptor4ai
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pytest
  - pysimplegui
  - pyinstaller
  - setuptools
  - wheel
  - mypy
  - pip:
      - types-setuptools