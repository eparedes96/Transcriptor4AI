name: transcriptor4ai
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pytest
  - pytest-mock
  - pyinstaller
  - requests
  - setuptools
  - wheel
  - mypy
  - tiktoken
  - ruff
  - pre-commit
  - pip:
      - types-setuptools
      - types-requests
      - customtkinter
      - tkinterdnd2
      - python-semantic-release
      # ------ INICIO DE MODIFICACIÓN: V2.0.0 Hybrid Tokenizer Deps ------
      - google-generativeai>=0.8.0
      - anthropic>=0.30.0
      - transformers>=4.40.0
      - mistral-common>=1.0.0
      # ------ FIN DE MODIFICACIÓN: V2.0.0 Hybrid Tokenizer Deps ------